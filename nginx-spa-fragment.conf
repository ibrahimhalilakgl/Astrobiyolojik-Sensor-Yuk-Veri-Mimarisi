# Nginx: React Router alt çizgili yollar (/rover_zekasi vb.) için dist köküne
# index.html düşürün; aksi halde doğrudan URL yenilemede 404 alırsınız.
#
# Örnek location bloğu (server içinde site root = frontend dist):
#
#   location / {
#       try_files $uri $uri/ /index.html;
#   }
